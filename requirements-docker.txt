# ─────────────────────────────────────────
# FightMind AI — Docker Live Service Requirements
# ─────────────────────────────────────────
# This file intentionally excludes data collection (BeautifulSoup, YoutubeTranscriptApi)
# and data science (Pandas) libraries to keep the Docker image strictly focused on serving the API.

# ── API Server ────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
httpx==0.28.1
python-dotenv==1.0.1
pyyaml==6.0.2

# ── Google Gemini ─────────────────────────
google-generativeai==0.8.3
google-genai==0.4.0
pydantic==2.10.5

# ── Groq Fallback LLM ─────────────────────
groq>=1.2.0

# ── RAG / Embeddings ──────────────────────
chromadb>=0.6.3
sentence-transformers==3.4.1
